{
    "machine": {
        "host": "$host",
        "username": "$username",
        "password": "$password"
    },
    "commands": [
        "sudo apt-get update",
        "curl -sL https://deb.nodesource.com/setup_7.x | sudo -E bash -",
        "sudo apt-get install -y nodejs",

        "npm install pm2 -g",

        "mkdir /logs",
        "npm --prefix /opt/octo-profile-service install",
        "pm2 stop octo-profile-service",
        "pm2 start /opt/octo-profile-service/src/app.js --name octo-profile-service -- --prod --port 9015",
        "sudo service nginx reload"
    ],
    "directories": [
        {
            "source": "$workspace/src",
            "destination": "/opt/octo-profile-service/src"
        },
        {
            "source": "$workspace/coverage",
            "destination": "/opt/octo-profile-service/coverage"
        }
    ],
    "files": [
        {
            "source": "$workspace/package.json",
            "destination": "/opt/octo-profile-service/package.json"
        },
        {
            "source": "$workspace/nginx.conf",
            "destination": "/etc/nginx/sites-enabled/octo-profile-service"
        }
    ]
}